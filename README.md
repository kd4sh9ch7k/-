 🎮 Игра "Виселица" (Hangman)

Простая консольная реализация классической игры "Виселица" на Python.

## 📁 Структура проекта
.
├── src/

│ └── game.py # Основная логика игры

├── main.py # Точка входа

├── words.txt # Файл со словами для игры

├── Dockerfile # Конфигурация Docker

├── docker-compose.yml # Docker Compose конфигурация

├── requirements.txt # Зависимости Python

├── .pre-commit-config.yaml # Конфигурация pre-commit

├── .gitignore # Игнорируемые файлы Git

├── pyproject.toml # Конфигурация Python инструментов

└── README.md # Документация


text

## 🚀 Быстрый старт

### Запуск локально

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd hangman-game
Создайте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
Установите зависимости:

bash
pip install -r requirements.txt
Запустите игру:

```bash
python main.py
Запуск через Docker
Соберите Docker образ:
```
``` bash
docker build -t hangman-game .
Запустите контейнер:
````
`` bash
docker run -it --rm hangman-game
Запуск через Docker Compose
bash
docker-compose up
🎯 Игровой процесс
Игра "Виселица" предлагает:

Угадывание слова по буквам

6 попыток на ошибки

Слова загружаются из файла words.txt

Русская локализация

Простой и понятный интерфейс

⚙️ Настройка
Добавление новых слов
Отредактируйте файл words.txt - каждое слово на новой строке:

txt
программирование
компьютер
алгоритм
виселица
Создание собственного файла со словами
Создайте файл со словами и укажите его путь при запуске игры.

🛠️ Разработка
Настройка pre-commit
Установите pre-commit хуки:

bash
pre-commit install
Запустите проверку всех файлов:

bash
pre-commit run --all-files
Форматирование кода
Проект использует Black для форматирования:

bash
black .
Проверка стиля кода
bash
flake8 src/
Проверка типов
bash
mypy src/
Проверка безопасности
bash
bandit -r src/
🐳 Docker
Сборка образа
bash
docker build -t hangman-game:latest .
Запуск с монтированием файла слов
bash
docker run -it --rm -v $(pwd)/words.txt:/app/words.txt hangman-game
Просмотр логов
bash
docker logs <container-id>
📦 Docker Compose
Проект включает docker-compose.yml для простого развертывания:

bash
# Запуск в фоновом режиме
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f
🔧 Конфигурация
Переменные окружения
Создайте файл .env:

env
# Пример переменных окружения
PYTHONUNBUFFERED=1
Настройка игры
Измените следующие параметры в src/game.py:

max_wrong: Количество попыток (по умолчанию: 6)

filename в load_words_from_file: Путь к файлу со словами
